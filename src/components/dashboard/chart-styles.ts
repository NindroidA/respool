/** Shared Recharts tooltip styles matching the app's design system. */
export const CHART_TOOLTIP_STYLE: React.CSSProperties = {
  background: "var(--bg-card)",
  border: "1px solid var(--border-default)",
  borderRadius: "8px",
  color: "var(--text-primary)",
  fontSize: "12px",
};
